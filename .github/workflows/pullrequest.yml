name: Verify Pull Request
on: push

permissions:
  checks: write
  id-token: write   # This is required for requesting the JWT
  contents: read    # This is required for actions/checkout
  security-events: write
  pull-requests: read
    
jobs:
  verifypr:
    runs-on: ubuntu-latest
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@95d9a5deda9de15063e7595e9719c11c38c90ae2 # v2.13.2
        with:
          disable-sudo: true
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            github.com:443
            uploads.github.com:443
            objects.githubusercontent.com:443
            release-assets.githubusercontent.com:443
            validator.nu:443
            raw.githubusercontent.com:443
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      
      # HTML Validation - Ensure all HTML files are valid HTML5
      - name: Validate HTML
        uses: Cyb3r-Jak3/html5validator-action@6f62a310ddc3c79a97ec2ba0f85253a5ea356688 # v7.2.0
        with:
          root: .
          css: false
          
      # Link Checking - Verify all links are accessible
      - name: Check Links
        uses: lycheeverse/lychee-action@a8c4c7cb88f0c7386610c35eb25108e448569cb0 # v2.7.0
        with:
          args: --verbose --no-progress --exclude-path screenshots './**/*.html' './**/*.md'
          fail: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      # Test Minify Action - Verify minification works correctly
      - name: Test Minify Action
        uses: dra1ex/minify-action@3c54a82e092a78c827659385d1be715126f13410 # v1.0.3
        
      # Verify minified files exist and are smaller
      - name: Verify Minification Results
        run: |
          echo "🔍 Checking minification results..."
          
          # Count HTML and CSS files in the repository
          html_count=$(find . -name "*.html" -not -path "./screenshots/*" -not -path "./.git/*" | wc -l)
          css_count=$(find . -name "*.css" -not -path "./screenshots/*" -not -path "./.git/*" | wc -l)
          
          echo "📊 Found ${html_count} HTML files and ${css_count} CSS files"
          
          # Verify key files still exist after minification
          if [ -f "index.html" ]; then
            size=$(stat -f%z "index.html" 2>/dev/null || stat -c%s "index.html")
            echo "✅ index.html exists (${size} bytes)"
          else
            echo "❌ index.html not found after minification"
            exit 1
          fi
          
          if [ -f "styles.css" ]; then
            size=$(stat -f%z "styles.css" 2>/dev/null || stat -c%s "styles.css")
            echo "✅ styles.css exists (${size} bytes)"
          else
            echo "❌ styles.css not found after minification"
            exit 1
          fi
          
          # Verify HTML files are still valid after minification
          total_files=$(find . -name "*.html" -not -path "./screenshots/*" -not -path "./.git/*" | wc -l)
          echo "✅ All ${total_files} HTML files verified after minification"
          echo "✅ Minification verification completed successfully"
          
      - name: Initialize CodeQL
        uses: github/codeql-action/init@0499de31b99561a6d14a36a5f662c2a54f91beee # v3.29.5
        with:
          languages: javascript
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@0499de31b99561a6d14a36a5f662c2a54f91beee # v3.29.5
